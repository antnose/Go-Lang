package main

// maps ->
func main() {
	// creating map
	// m := make(map[string]string)
	// setting an element
	// m["name"] = "golang"
	// m["area"] = "backend"

	// get an element
	// fmt.Println(m["name"], m["area"], m["ASDF"])

	// important: if key does not exists in the map then it returns zero value
	// fmt.Println(m["phone"])

	// m := make(map[string]int)
	// m["age"] = 30
	// m["price"] = 40
	// fmt.Println(m["age"])
	// fmt.Println(m["phone"])

	// fmt.Println(len((m)))
	// delete(m, "price")
	// fmt.Println(m)
	// clear(m)
	// fmt.Println(m)

}
